\ProvidesPackage{codehigh}

% A lightweight substitute for the missing package.
% Uses listings for syntax highlighting.

\RequirePackage{listings}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  numbers=left,
  numberstyle=\tiny,
  keywordstyle=\bfseries,
  commentstyle=\itshape
}

\endinput
